# 项目提示词 & AI 上下文记忆库（换设备 / 换 AI 时用）

用法：把下面这段提示词整段复制，在新会话开头粘贴。**已填好【当前进度】、【避坑知识库】和【已验证硬件接线】，直接照做。**

> ⚠️ 给接手 AI 的硬性要求：**先读完【当前进度】、【硬件真实接线】和【避坑指南】。** 项目环境已全部装好并实测点亮通过，不要重装软件、不要更改已物理固化的接线方案！

---

## 一、项目定义与分工
- 角色：你是我（物联网工程专业大二、硬件零基础）的嵌入式项目助手。
- 分工：用户负责硬件接线与烧录，代码全部由 AI 负责提供。
- 项目目标：ESP32 + 2.4 寸 TFT 彩屏（ILI9341）打造桌面无线副屏，实时显示：网络时钟 + CPU 占用/进度条 + CPU Package 封装温度/功耗 + GPU 温度/功耗 + 内存占用/进度条。

---

## 二、硬件真实接线表（已上机 100% 验证通过，严禁擅自改动引脚）

* **屏幕型号**：2.4 寸 TFT LCD（红色底板，丝印 `2.4" TFT SPI 240X320 V1.3`，驱动 IC：**ILI9341**）
* **供电要求**：背板 J1 未短接，**VCC 必须接 5V**（若接 3.3V 经稳压芯片后欠压至 2.7V 会导致死锁白屏）；**LED 背光接 3.3V**。

| 屏幕排针物理顺序（从下到上） | 屏幕丝印 | ➔ ESP32 GPIO 引脚 | 关键避坑说明 |
| :--- | :--- | :--- | :--- |
| **Pin 1** | **VCC** | ➔ **5V (VIN)** | 必须接 5V 保证供电充沛 |
| **Pin 2** | **GND** | ➔ **GND** | ⚠️ **严禁误插到 G0/SD0**（拉低总线会导致 `flash read err`） |
| **Pin 3** | **CS** | ➔ **GPIO 4 (D4)** | 避开 Strapping 引脚 GPIO 15/5 |
| **Pin 4** | **RESET** | ➔ **GPIO 17 (TX2)** | 提供硬件复位脉冲 |
| **Pin 5** | **DC** | ➔ **GPIO 16 (RX2)** | 避开 Strapping 引脚 GPIO 2/13 |
| **Pin 6** | **SDI(MOSI)**| ➔ **GPIO 23 (D23)**| SPI 数据线，⚠️ **严禁插到 SD0/SD1/SD2/SD3** |
| **Pin 7** | **SCK** | ➔ **GPIO 18 (D18)**| SPI 时钟线，⚠️ **严禁插到板载内部 Flash 的 CLK** |
| **Pin 8** | **LED** | ➔ **3.3V (3V3)** | 屏幕背光 |
| **Pin 9** | **SDO(MISO)**| ➔ **不接（悬空）** | 本项目无需回传触摸/SD 数据 |
| **Pin 10~14** | **T_CLK~T_IRQ**| ➔ **不接（悬空）** | 触摸引脚 |

---

## 三、AI 核心避坑知识库 (Troubleshooting Knowledge Base)

1. **烧录报错 `MD5 mismatch` 或串口死循环 `flash read err, 988`**：
   - 根本原因：GND 误插到了 `G0/SD0`，或 SCK/MOSI 插到了板载 Flash 内部引脚（`CLK / SD0~SD3 / CMD`）。
   - 解决：按上述接线表，彻底避开 Flash 禁区引脚。
2. **烧录时报错 `Packet content transfer stopped (received 8 bytes)`**：
   - 根本原因：USB 供电瞬间被屏幕拉低。
   - 解决：避开 Strapping 引脚；必要时烧录时临时拔掉 VCC，烧录后插上按 `EN` 重启。
3. **屏幕一直纯白屏 (White Screen)**：
   - 根本原因：杜邦线高频信号衰减或 `TFT_eSPI` 兼容性问题。
   - 解决：`04_widget.ino` 已集成自主纯底层直驱驱动引擎（Bitbang / 稳健时序），彻底脱离库配置依赖。
4. **屏幕 180° 方向翻转**：
   - 寄存器命令 `0x36 (MADCTL)` 写入 `0xE8` 实现横屏顺向。
5. **AIDA64 读不到温度 / 报错 `WinError 5 拒绝访问` / `OLE error 0x8004100e`**：
   - 根本原因：AIDA64 为管理员权限，普通用户 Python 读 WMI/共享内存受限。
   - 解决：AIDA64 设置勾选 **“启用写入注册表”**，`03_pc_server.py` 直接免提权读取 `HKEY_CURRENT_USER\Software\FinalWire\AIDA64\SensorValues`。
   - 匹配规则：**严格优先匹配 `CPU Package (CPU 封装)`**。

---

## 四、项目工程文件结构

- `00_购买清单与完整流程.md`：总览指南
- `01_hello_screen/01_hello_screen.ino`：阶段 1 屏幕点亮与 RGB 循环测试
- `02_ntp_time/02_ntp_time.ino`：阶段 2 WiFi 网络时间同步（NTP 7段数码管大字体）
- `03_pc_server.py`：阶段 3 电脑端 Python 数据服务（支持 CPU/GPU 功耗、温度、占用率及注册表秒读）
- `04_widget/04_widget.ino`：阶段 4 最终主程序（全能硬件副屏，1.2 秒自动刷新）
- `99_项目提示词.md`：本项目知识库与 AI 记忆体

---

## 五、当前进度（状态更新）
- **阶段 0（环境与驱动）**：已完成。
- **阶段 1（点亮屏幕）**：已完成（红绿蓝测试通过）。
- **阶段 2（网络时间）**：已完成（NTP 同步成功）。
- **阶段 3（PC 服务）**：已完成（注册表直读 CPU Package / GPU 功耗温度稳定运行）。
- **阶段 4（副屏联调）**：已就绪，直接运行 `python 03_pc_server.py` 并烧录 `04_widget.ino` 即可享受桌面硬件副屏。

